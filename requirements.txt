# Dependencies for the GRPO training notebook (tunix_reasoning_trainer.ipynb).
#
# NOTE: The reward functions (reasoning_rewards.py) and the data generator
# (generate_training_data.py) use only the Python standard library, so you do
# NOT need to install anything to run or test those (see requirements-dev.txt).
#
# The notebook installs this stack itself in its first cell. These pins mirror
# the official Tunix GRPO demo; on Colab/Kaggle most are pre-installed and JAX
# auto-detects the TPU/GPU runtime.

# Data, tokenization, and utilities.
kagglehub>=0.2.0
tensorflow>=2.15.0
tensorflow_datasets>=4.9.0
grain>=0.2.0
transformers>=4.40.0
huggingface_hub>=0.23.0
datasets>=2.19.0
numpy>2

# JAX + Tunix post-training stack (installed from source in the notebook):
#   pip install git+https://github.com/jax-ml/jax
#   pip install git+https://github.com/google/tunix
#   pip install git+https://github.com/google/qwix
#   pip install git+https://github.com/google/flax
